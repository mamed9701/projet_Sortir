{{ form_start(form) }}
   {{ form_row(form.villes_no_ville) }}
<a href="{{ path('ville_new') }}" class="btn btn-success">Ajouter une ville <i class="fas fa-plus"></i></a>
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
