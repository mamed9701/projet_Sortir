<form method="post" action="{{ path('user_controller_delete', {'id': user_controller.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_controller.id) }}">
    <button class="btn">Delete</button>
</form>
